// REGRA #4: lugar unico para a fonte de custos quando existir.
// ADIADO: ainda nao ha tabela de custos no modelo de dados (ver plano-mestre, feature 11).
// Quando houver a tabela (ex.: 'custos'), implementar listCosts() aqui com um SELECT via dbQuery,
// no mesmo padrao dos demais data/*.js.
export async function listCosts() {
  return []
}
