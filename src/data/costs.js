// REGRA #4: lugar unico para a fonte de custos quando existir.
// ADIADO: ainda nao ha tabela/lista de custos no modelo de dados (ver plano-mestre, feature 11).
// Quando houver uma lista (ex.: 'Custos'), implementar listCosts() aqui no mesmo padrao dos demais.
export async function listCosts() {
  return []
}
